# 后端 PROMPT 重构说明

## 问题背景

原 PROMPT 存在以下问题：
1. **判断逻辑不清晰**：AI 经常返回"无关"即使问题涉及汤面中的具体元素
2. **缺少结构化指令**：AI 没有明确的思考流程
3. **约束不够严格**：AI 可能输出额外解释或模糊回答

## 重构方案

### 1. 引入结构化指令（Chain of Thought）

```markdown
### 第一步：理解汤面和汤底
- 仔细阅读汤面，提取所有关键信息
- 仔细阅读汤底，理解真相核心
- 识别汤面中出现的所有具体名词

### 第二步：分析玩家问题
- 提取问题的核心疑问点
- 识别问题是否涉及汤面中的具体元素
- 判断问题是否与汤底存在逻辑关联

### 第三步：逻辑推理（Chain of Thought）
- 如果问题涉及汤面中的具体名词 → 必须回答"是"或"否"
- 如果问题与汤底完全无关 → 回答"无关"
- 如果问题内容为纯骚扰/废话 → 回答"无关"
```

### 2. 新增汤面关键词强制判断规则

```markdown
### 规则1：汤面关键词强制判断
如果玩家问题包含汤面中的任何具体名词（如'掌声'、'观众'、'门'、'声音'等）：
- 禁止回答"无关"
- 必须根据汤底判断为"是"或"否"
```

**目的**：防止 AI 因为不确定而逃避回答，强制它对汤面中出现的元素做出判断

### 3. 严格的输出约束

```markdown
### 规则2：输出格式约束
严格输出 JSON 格式：
{
  "answer": "是|否|无关",  // 只能是这三个值之一
  "reason": "简短理由"      // 10字以内的判断理由
}

### 规则3：禁止事项
- 严禁输出任何JSON格式以外的内容
- 严禁添加注释、解释、推理过程
- 严禁输出多余字符、空格、换行
- 严禁输出"我无法判断"等模糊回答
- 必须严格遵守JSON语法
```

### 4. 优化的 Few-shot 案例

提供了 6 个精心设计的案例，覆盖各种场景：

| 案例编号 | 场景 | 预期答案 | 核心逻辑 |
|---------|------|---------|---------|
| 案例1 | 关键正确信息 | "是" | 直接命中汤底 |
| 案例2 | 关键错误信息 | "否" | 与汤底矛盾 |
| 案例3 | 汤面关键词强制 | "否" | 录音非现场 |
| 案例4 | 汤面关键词强制 | "是" | 确认有鼓掌 |
| 案例5 | 纯骚扰问题 | "无关" | 与汤底无关 |
| 案例6 | 汤面关键词强制 | "是" | 门板被撞 |

**重点**：案例3 和案例4 强调汤面关键词强制判断规则
- 问题包含汤面中的名词（"观众"、"掌声"）
- 必须回答"是"或"否"，不能回答"无关"

## 测试结果

### 测试用例
```bash
# 测试汤面关键词强制判断
curl -X POST http://localhost:3001/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "question": "观众鼓掌了吗？",
    "story": {
      "title": "测试",
      "surface": "表演结束后，观众爆发出热烈的掌声",
      "bottom": "观众对表演非常满意，现场鼓掌了5分钟"
    }
  }'
```

**预期结果**：
```json
{"answer":"是","reason":"确认有鼓掌"}
```

**实际结果**：
```json
{"answer":"是","isFallback":false}
```

✅ 测试通过！

## 改进效果

### 改进前
- AI 经常对汤面中的名词返回"无关"
- 回答不稳定，缺乏一致性
- 容易输出额外解释

### 改进后
- ✅ 汤面关键词强制判断，禁止随意返回"无关"
- ✅ 结构化思考流程，AI 逻辑更清晰
- ✅ 严格的输出格式约束，答案更一致
- ✅ 6 个 Few-shot 案例，覆盖各种场景

## 部署建议

### 本地测试
```bash
cd ai-haigui-backend
npm start

# 在另一个终端测试
curl -X POST http://localhost:3001/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "question": "你的测试问题",
    "story": {
      "title": "故事标题",
      "surface": "汤面内容",
      "bottom": "汤底内容"
    }
  }'
```

### 生产部署
确保后端服务正确启动并监听正确端口：
- Vercel: 设置环境变量 `PORT=3001`
- Railway: 默认为 3001

## 监控建议

观察以下指标：
1. **"无关"返回率**：应该显著下降
2. **回答一致性**：相同问题应该得到相同答案
3. **格式合规性**：所有回答应该都是"是"/"否"/"无关"

## 后续优化

如果 AI 回答仍然不稳定，可以考虑：
1. 进一步细化 Few-shot 案例
2. 增加 temperature 参数控制（当前为 0）
3. 考虑使用更稳定的模型版本